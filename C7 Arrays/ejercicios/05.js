function agregarItemAlComienzoDelArray(array, elemento) {
  // Agrega el "elemento" al comienzo del arreglo recibido.
  // Retorna el arreglo.
  // Tu código:
   let recorreElemento;
  for recorreElemento=(i = 0,i === elemento,i++){
array.push(0); 
  return array;

  }


}

module.exports = agregarItemAlComienzoDelArray;
